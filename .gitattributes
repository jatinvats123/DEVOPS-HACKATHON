# Normalize all text files to LF in the repository so Windows/macOS/Linux and CI
# agree on line endings (no CRLF churn in diffs).
* text=auto eol=lf

# Binary assets — never normalize
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.webp binary
*.woff binary
*.woff2 binary
