{
  "name": "watchtower",
  "version": "1.0.0",
  "private": true,
  "description": "WatchTower — HTTP/API uptime monitoring platform (Express + React). Root-level scripts orchestrate the Backend and Frontend packages.",
  "engines": {
    "node": ">=20 <23"
  },
  "scripts": {
    "install:all": "npm --prefix Backend install && npm --prefix Frontend install",
    "dev:backend": "npm --prefix Backend run dev",
    "dev:frontend": "npm --prefix Frontend run dev",
    "build": "npm --prefix Frontend run build",
    "start": "npm --prefix Backend start",
    "lint": "npm --prefix Backend run lint && npm --prefix Frontend run lint",
    "lint:fix": "npm --prefix Backend run lint:fix && npm --prefix Frontend run lint:fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "npm --prefix Backend test",
    "test:coverage": "npm --prefix Backend run test:coverage",
    "audit": "node scripts/audit-gate.mjs Backend Frontend",
    "badge:coverage": "node scripts/coverage-badge.mjs",
    "docker:build": "docker build -f Backend/Dockerfile -t watchtower-backend:local . && docker build -f Frontend/Dockerfile -t watchtower-frontend:local ./Frontend",
    "docker:up": "docker compose up -d --build",
    "docker:down": "docker compose down -v"
  },
  "devDependencies": {
    "prettier": "^3.4.2"
  }
}
